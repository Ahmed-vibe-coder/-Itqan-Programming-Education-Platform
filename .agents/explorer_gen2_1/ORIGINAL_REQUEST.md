## 2026-08-01T10:32:15Z
<USER_REQUEST>
You are Explorer Gen2 1. Your task is to investigate and produce an exact fix strategy report for the Forensic Audit evidence in Milestone 3.

Working directory: d:\@vibcoding\ai\.agents\explorer_gen2_1\
Project root: d:\@vibcoding\ai

### Background & Forensic Audit Evidence Report (Verbatim)
Forensic Auditor 2 reported an INTEGRITY VIOLATION with the following evidence:

1. Empirical Test Suite Failures (`src/tests/auth.test.ts`):
   - Test `validates invitation code using authService.validateInvitation`: `authService.validateInvitation` throws error `'كود الدعوة غير صحيح أو غير مفعّل.'`, whereas test expects substring `'كود الدعوة غير صحيح.'`. Examine `src/services/authService.ts` and `src/tests/auth.test.ts`. Determine whether to update test assertion or service error message so both align perfectly.
   - Test `rejects invalid invitation codes with an error`: AssertionError: expected [Function] to throw error including `'كود الدعوة غير صحيح.'` but got `'كود الدعوة غير صحيح أو غير مفعّل.'`.
   - Test `verifies that codes generated by teacherService.createInvitation pass validation`: `teacherService.createInvitation` fails with PostgreSQL UUID syntax error (`22P02`) when passed dummy non-UUID string `"grp-1"`. Examine `src/services/teacherService.ts` and `src/tests/auth.test.ts` to ensure valid UUIDs (e.g. `"g1000000-0000-0000-0000-000000000001"` or proper UUID format) are passed/handled.

2. `src/app/providers/AuthProvider.tsx` LocalStorage Primitive JSON Leak:
   - Line 59: `JSON.parse(storedUser)` does NOT throw an exception for primitive valid JSON strings (e.g. `'99999'` or `'true'`).
   - Fix strategy: Explicitly check `if (typeof parsed !== 'object' || parsed === null)` and purge `localStorage.removeItem('nawa_mock_session')` if primitive or invalid object structure.

3. `src/app/guards/RoleGuard.tsx` Unrecognized Role Leak:
   - Line 50: `return <Navigate to={role === 'student' ? '/app' : '/teacher'} replace />;`.
   - When `role` is an unrecognized non-student string e.g. `'guest'`, it redirects to `/teacher`.
   - Fix strategy: Ensure `if (!role || !allowedRoles.includes(role))` redirects unconditionally to `/login` (or `/unauthorized`) when user role is not allowed or unrecognized.

### Instructions:
1. Examine `src/tests/auth.test.ts`, `src/services/authService.ts`, `src/services/teacherService.ts`, `src/app/providers/AuthProvider.tsx`, and `src/app/guards/RoleGuard.tsx`.
2. Formulate precise, line-by-line file edit instructions for the Worker.
3. Write your report to `d:\@vibcoding\ai\.agents\explorer_gen2_1\fix_strategy.md` and `d:\@vibcoding\ai\.agents\explorer_gen2_1\handoff.md`.
4. Send a message to parent orchestrator when complete with summary and links to the handoff files.
</USER_REQUEST>
